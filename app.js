let numberToGuess = Math.ceil(Math.random() * 100) + 1;
const turnNumber = 1;
const guesses = document.querySelectorAll('.btn btn-outline-light');
const lowOrHigh = document.querySelector('.lowOrHigh');

const guess
